import React from 'react';

const Header = () => {
  return (
    <header style={{display: "flex", justifyContent: "center", alignItems: "center", backgroundColor: "red", height: "100px"}}>
      <h1>shashi</h1>
    </header>
  );
};

export default Header;
